# GTM AI Agent 환경 변수
# GTM 자격 정보(account_id, container_id)는 UI 또는 CLI 실행 시 입력합니다.
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Playwright Chromium: 비우거나 0이면 창 표시(기본). 1/true/yes면 headless.
# GTM_AI_HEADLESS=0
